{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && node src/index.js"
  },
  "cron": [
    {
      "name": "create-yearly-balances",
      "schedule": "0 0 1 1 *",
      "command": "node scripts/create-yearly-balances.js"
    }
  ]
}